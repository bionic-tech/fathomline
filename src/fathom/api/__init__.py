"""FastAPI routers, Pydantic schemas, and dependencies.

Placeholder for the modular-monolith layout (standards/18 §6). The read surface and the
write (remediation) surface are deliberately separate route groups with different auth
dependencies (ADD 01 §"API Endpoints"). Built in Stage 2 (transport + catalogue) onward.
"""
