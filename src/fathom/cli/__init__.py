"""Thin CLI over the API client (standards/18 §6).

Placeholder. v1 CLI keys are read/report/dry-run only — execute-capable API keys are
deferred (owner Decision D1, closed_issues.md). Built once the API surface exists.
"""
